#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int (i)=0;(i)<(n);(i)++)

int n, a[30];

int main() {
	while (scanf("%d",&n) && n) {
		int a[n] = {};
		a[0] = n;
		printf("%d\n",n);
		
	}
}